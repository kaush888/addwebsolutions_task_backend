// ---- Base URLS ----
var HOST_NAME = 'localhost';
var PORT = 3001;

module.exports ={

    // ---- JWT ----
    JWT_ENCRYPTION_KEY : "Student.Crud.Secret",

    // ---- Base URLS ----
    HOST_NAME : HOST_NAME,
    PORT : PORT,

} 